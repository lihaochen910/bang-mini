--------------------------------------------------------------------
---@class IRenderSystem : ISystem
--- A system that leverages the engine implementations of render functionality.
CLASS: IRenderSystem( ISystem )
    :META {
        interface = true
    }
